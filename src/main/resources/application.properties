# H2 Database Settings (Deactivated)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.datasource.url=jdbc:h2:mem:ipamdb

# MySQL Database Settings
spring.datasource.url=jdbc:mysql://localhost:3306/ipam_db?serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=hoict

# MyBatis Settings
mybatis.mapper-locations=classpath:mappers/*.xml
mybatis.configuration.cache-enabled=true

# SQL Logging
logging.level.com.jules.ipam.dao=TRACE

# Internationalization
spring.messages.basename=messages

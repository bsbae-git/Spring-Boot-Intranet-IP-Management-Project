<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ipam">
    <insert id="insert" parameterType="com.example.ipam.vo.IpAddressVO">
        INSERT INTO ip_addresses (ip_address, mac_address, hostname, status, last_seen)
        VALUES (#{ipAddress}, #{macAddress}, #{hostname}, #{status}, #{lastSeen})
    </insert>

    <update id="update" parameterType="com.example.ipam.vo.IpAddressVO">
        UPDATE ip_addresses
        SET mac_address = #{macAddress},
            hostname = #{hostname},
            status = #{status},
            last_seen = #{lastSeen}
        WHERE ip_address = #{ipAddress}
    </update>

    <select id="findByIpAddress" parameterType="string" resultType="com.example.ipam.vo.IpAddressVO">
        SELECT
            id,
            ip_address,
            mac_address,
            hostname,
            status,
            last_seen
        FROM
            ip_addresses
        WHERE
            ip_address = #{ipAddress}
    </select>

    <select id="findAll" resultType="com.example.ipam.vo.IpAddressVO">
        SELECT
            id,
            ip_address,
            mac_address,
            hostname,
            status,
            last_seen
        FROM
            ip_addresses
    </select>
</mapper>
